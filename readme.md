# ✈️ Instruções de Execução do Projeto — AV2

Este projeto é uma aplicação **frontend** desenvolvida em **React + TypeScript + Tailwind CSS**.  
Ela simula um sistema de gestão de aeronaves, com **tela de login, página inicial (Home)** e **painel (Dashboard)**.

------------------------------------------------------------------------

## Passo 1 — Clonar o repositório

Clone o repositório do projeto para o seu computador utilizando **Git**:

```bash
🔗 [https://github.com/TaylorSilva2/AV2.git](https://github.com/TaylorSilva2/AV2.git)
```
## Passo 2 — Acessar a pasta do projeto

Entre na pasta **AV2** utilizando o **Git Bash**, **CMD** ou **PowerShell**:

```bash
cd AV2
```

## Passo 3 — Instalar as dependências

Execute o comando abaixo para instalar todas as dependências do projeto:

```bash
npm install
```

💡 **Este comando instalará automaticamente o React, TypeScript, Tailwind e o React Router DOM.**

## Passo 4 — Executar o projeto

Após a instalação, inicie o servidor de desenvolvimento com o comando:

```bash
npm run dev
```

## Passo 5 — Acessar no navegador

Abra o navegador e acesse o seguinte link:

🔗 [http://localhost:5173/](http://localhost:5173/)

## Passo 6 — Fazer login

A primeira tela exibida será a de **Login**.

Digite **qualquer e-mail** e **qualquer senha** (valores aleatórios são aceitos).

Clique no botão **“Entrar”**.

Você será redirecionado automaticamente para a página **Home**.  
A partir dela, é possível navegar até o **Painel (Dashboard)**.